<section class="section account">
  <div class="account__auth-wrapper">
    <h1 class="account__auth-wrapper-heading type-heading-1">
      {{ 'customer.activate_account.title' | t }}
    </h1>
    <p class="mb4">{{ 'customer.activate_account.subtext' | t }}</p>

    {% form 'activate_customer_password' %}
      {{ form.errors | default_errors }}

      <div class="relative">
        <input
          type="password"
          name="customer[password]"
          id="CustomerPassword"
          class="input"
          placeholder="{{ 'customer.activate_account.password' | t }}"
        >
        <label for="CustomerPassword" class="label--above">
          {{ 'customer.activate_account.password' | t }}
        </label>
      </div>

      <div class="relative">
        <input
          type="password"
          name="customer[password_confirmation]"
          id="CustomerPasswordConfirmation"
          class="input"
          placeholder="{{ 'customer.activate_account.password_confirm' | t }}"
        >
        <label for="CustomerPasswordConfirmation" class="label--above">
          {{ 'customer.activate_account.password_confirm' | t }}
        </label>
      </div>

      <input
        type="submit"
        class="bttn bttn--full mb2"
        value="{{ 'customer.activate_account.submit' | t }}"
      >
      <input
        type="submit"
        class="bttn bttn--full bttn--secondary"
        name="decline"
        value="{{ 'customer.activate_account.cancel' | t }}"
      >
    {% endform %}
  </div>
</section>